# Railway 배포 준비 완료 ✅ (monoparty 레포)

## 변경된 파일
- `Dockerfile` — game-server multi-stage build
- `railway.json` — Railway deploy config  
- `game-server/src/index.ts` — PORT env → 8080
- `game-server/package.json` — type: module 추가
- `.gitignore` — cleanup

## Railway 설정 방법

1. https://railway.app → `monoparty` 리포지토리 연결
2. `game-server/` 디렉토리 선택
3. Environment Variables: `PORT` → `8080`
4. Deploy

## 도메인
`https://monoparty-production.up.railway.app:8080`

## 프론트엔드 (Vercel) 환경변수

Vercel 대시보드 → Settings → Environment Variables:
- `VITE_GAME_SERVER_URL` → `wss://monoparty-production.up.railway.app:8080`
- `VITE_SUPABASE_URL` → 기존 값 유지
- `VITE_SUPABASE_ANON_KEY` → 기존 값 유지
